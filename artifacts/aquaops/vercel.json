{
  "framework": null,
  "buildCommand": "pnpm --filter @workspace/aquaops run build && cp -r artifacts/aquaops/dist ./deploy-output",
  "outputDirectory": "deploy-output",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}