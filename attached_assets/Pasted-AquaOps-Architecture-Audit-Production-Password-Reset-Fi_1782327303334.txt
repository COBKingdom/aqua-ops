AquaOps Architecture Audit – Production Password Reset Fix

IMPORTANT:

Do NOT make any code changes.
Do NOT deploy anything.
Do NOT modify Vercel settings.
Do NOT commit anything.
Analysis and implementation plan only.

Current Facts Confirmed:

1. Production URL:
   https://aqua.trueops.app

2. Vercel Settings:

* Framework: Next.js
* Root Directory: /

3. Repository Structure:

Root Application:
app/
app/auth/
app/auth/forgot-password/page.tsx
app/auth/reset-password/page.tsx

Migrated Replit Application:
artifacts/aquaops/
artifacts/aquaops/src/
artifacts/aquaops/src/components/screens/forgot-password.tsx
artifacts/aquaops/src/components/screens/reset-password.tsx

4. Critical Discovery:

The root Next.js files currently deployed by Vercel are:

app/auth/forgot-password/page.tsx
app/auth/reset-password/page.tsx

Both files are 0 bytes (empty).

5. Replit Implementation:

The actual Forgot Password and Reset Password implementation currently exists only in:

artifacts/aquaops/src/components/screens/forgot-password.tsx
artifacts/aquaops/src/components/screens/reset-password.tsx

6. Production Behaviour:

* Forgot Password email sends successfully.

* Reset email contains:
  redirect_to=https://aqua.trueops.app

* Visiting:
  https://aqua.trueops.app/reset-password

returns:

404 - This page could not be found.

7. Verified Replit Code:

The Vite router contains:

<Route path="/forgot-password" component={ForgotPasswordScreen} />
<Route path="/reset-password" component={ResetPasswordScreen} />

The forgot-password implementation generates:

window.location.origin + "/reset-password"

which is correct.

Objective:

Determine the safest production approach without breaking existing AquaOps users currently trialling the platform.

Questions to Answer:

1. Which application should be considered the true production source of truth:

   * Root Next.js application
   * Replit Vite application in artifacts/aquaops

2. Is Vercel currently deploying a different application than the one Replit has been modifying?

3. What is the safest approach to get Forgot Password and Reset Password working in production?

4. Should we:
   Option A:
   Port the forgot-password and reset-password functionality from the Vite application into the root Next.js application?

   Option B:
   Change Vercel to deploy artifacts/aquaops instead?

5. Which option carries the lowest risk to existing AquaOps users?

6. Provide a detailed step-by-step implementation plan.

7. Identify any hidden risks, routing issues, authentication issues, or deployment risks before any code changes are made.

Expected Deliverable:

A complete audit report and recommended implementation plan only.

No code changes.
No commits.
No deployment.
No Vercel changes.
Analysis only.
